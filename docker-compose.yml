version: '3.2'
services:
  app:
    build: .
    container_name: Todo-app
    networks:
    - todo-network
    environment:
      DB_HOST: database-1.cczouquwokjr.us-east-1.rds.amazonaws.com
      DB_USER: admin
      DB_PASS: Admin1232003
      DB_NAME: todo_app
      DB_PORT: 3306

    ports:
      - "8081:80"
      - "8082:80"
networks:
  todo-network:


  # depends_on : 
  #  - db

  # volumes:
  # - .:/var/www/html

#  db:
#   image: mysql:8.0
#   container_name: todo_db
#   restart: always
#   environment:
#       MYSQL_ROOT_PASSWORD: admin123
#       MYSQL_DATABASE: todo_app
#       MYSQL_USER: todouser
#       MYSQL_PASSWORD: admin123
#   ports:
#       - "3306:3306"
#   volumes:
#       - db_data:/var/lib/mysql        # persist DB data
#       - ./schema.sql:/docker-entrypoint-initdb.d/schema.sql  # auto-import schema

# volumes:
#   db_data:
